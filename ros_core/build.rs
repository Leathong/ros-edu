use std::env;
fn main() {
    // let current_dir = env::var("CARGO_MANIFEST_DIR").unwrap();
    // println!("cargo:rerun-if-changed={}/src/linker.ld", &current_dir);
    // println!("cargo:rustc-link-arg=-T{}/src/linker.ld", &current_dir);
}